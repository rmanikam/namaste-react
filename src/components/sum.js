import { faWordpress } from "@fortawesome/free-brands-svg-icons";

export const sum = (a, b) => {
  return a + b;
};

// Headers.test.js
// Headers.test.ts
// Headers.spec.js
// Headers.spec.ts are all testing files

// _ _ (2 times underscore is called dunder tests)

// we use dunder method as reserve word

// so that nobody creates folder name like this __tests
